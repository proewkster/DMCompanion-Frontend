import { DtoListCharacters } from './dto-list-characters';

describe('DtoListCharacters', () => {
  it('should create an instance', () => {
    expect(new DtoListCharacters()).toBeTruthy();
  });
});
