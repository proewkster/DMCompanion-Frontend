import { DtoUpdatePassword } from './dto-update-password';

describe('DtoUpdatePassword', () => {
  it('should create an instance', () => {
    expect(new DtoUpdatePassword()).toBeTruthy();
  });
});
