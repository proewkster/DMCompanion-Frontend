import { DynamicFormControl_Base } from "./dynamicformcontrol-base";

export class DynamicFormControl_Number extends DynamicFormControl_Base<number> {
    // Define default properties
    controlType = 'number';
}