import { DynamicFormControl_Base } from "./dynamicformcontrol-base";

export class DynamicFormControl_Text extends DynamicFormControl_Base<string> {
    // Define default properties
    controlType = 'textbox';
}